name: Pull Request Bot

on:
  pull_request:
    types: [closed]

jobs:
  remove-status-label:
    name: Remove `S-waiting-for-review` label
    runs-on: ubuntu-latest
    steps:
      - name: Remove the label
        run: |
          curl -X DELETE \
          https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/labels/S-waiting-for-review \
          --header 'Authorization: token ${{ secrets.GITHUB_TOKEN_2 }}'
